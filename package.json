{
  "name": "tech-blogs",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "format": "prettier --write ./data.json",
    "lint": "prettier --write ./data.json"
  },
  "dependencies": {
    "normalize.css": "^8.0.1",
    "nuxt": "3.20.2"
  },
  "devDependencies": {
    "less": "^4.0.0",
    "prettier": "2.4.1"
  }
}
